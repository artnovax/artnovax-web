services:
  mongo:
    image: mongo:7
    container_name: artnovax-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - artnovax-mongo-data:/data/db

volumes:
  artnovax-mongo-data:
